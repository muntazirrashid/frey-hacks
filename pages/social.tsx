import Head from "next/head";
import { Fragment } from "react";

type Props = {};

const Social = (props: Props) => {
  return (
    <Fragment>
      <Head>
        <title>Social</title>
      </Head>
    </Fragment>
  );
};

export default Social;
