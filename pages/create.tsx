import Head from "next/head";
import { Fragment } from "react";

type Props = {};

const create = (props: Props) => {
  return (
    <Fragment>
      <Head>
        <title>Create</title>
      </Head>
    </Fragment>
  );
};

export default create;
